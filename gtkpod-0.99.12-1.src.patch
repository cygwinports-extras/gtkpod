diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x Makefile.in -x makefile.in -x configure -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/gtkpod-0.99.12/configure.in src/gtkpod-0.99.12/configure.in
--- origsrc/gtkpod-0.99.12/configure.in	2007-12-17 22:37:46.000000000 -0600
+++ src/gtkpod-0.99.12/configure.in	2007-12-22 19:36:39.921875000 -0600
@@ -190,8 +190,8 @@
 dnl Check for libvorbisfile
 AC_ARG_WITH(ogg, AC_HELP_STRING([--without-ogg], [Disable Ogg/Vorbis support]))
 if test "x$with_ogg" != "xno"; then
-    AC_SEARCH_LIBS(ov_open, ["vorbisfile"],
-        [AC_CHECK_HEADER(vorbis/vorbisfile.h, have_ogg=yes, have_ogg_header=no)], have_ogg=no)
+    AC_CHECK_LIB(vorbisfile, ov_open,
+        [AC_CHECK_HEADER(vorbis/vorbisfile.h, LIBS="-lvorbisfile -lvorbis $LIBS" have_ogg=yes, have_ogg_header=no)], have_ogg=no)
     if test "x$have_ogg_header" = "xno"; then
         have_ogg="no"
         AC_MSG_WARN([vorbis/vorbisfile.h cannot be found or is unusable. Check your ogg/vorbis installation.])
diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x Makefile.in -x makefile.in -x configure -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/gtkpod-0.99.12/src/file_convert.c src/gtkpod-0.99.12/src/file_convert.c
--- origsrc/gtkpod-0.99.12/src/file_convert.c	2007-12-17 22:37:43.000000000 -0600
+++ src/gtkpod-0.99.12/src/file_convert.c	2007-12-22 19:19:18.515625000 -0600
@@ -216,7 +216,7 @@
     gchar *fname_root;      /* filename root of converted file          */
     gchar *fname_extension; /* filename extension of converted file     */
     GPid  pid;              /* PID of child doing the conversion        */
-    gint  stderr;           /* stderr of child doing the conversion     */
+    gint  std_err;          /* stderr of child doing the conversion     */
     Track *track;           /* for reference, don't access inside threads! */
     iTunesDB *itdb;         /* for reference, don't access inside threads! */
     gint  threadnum;        /* number of thread working on this track   */
@@ -229,7 +229,7 @@
     gchar *title;
     gchar *genre;
     gchar *year;
-    gchar *comment;
+    gchar *_comment;
     /* needed for transfering */
     gchar *dest_filename;
     gchar *mountpoint;
@@ -1062,7 +1062,7 @@
     ctr->title     = g_strdup (track->title);
     ctr->genre     = g_strdup (track->genre);
     ctr->year      = g_strdup (etr->year_str);
-    ctr->comment   = g_strdup (track->comment);
+    ctr->_comment  = g_strdup (track->comment);
     ctr->valid = TRUE;
 
     if (!etr->pc_path_locale || (strlen (etr->pc_path_locale) == 0))
@@ -1241,7 +1241,7 @@
     g_free (ctr->title);
     g_free (ctr->genre);
     g_free (ctr->year);
-    g_free (ctr->comment);
+    g_free (ctr->_comment);
     if (ctr->gio_channel)
     {
 	g_io_channel_unref (ctr->gio_channel);
@@ -2292,7 +2292,7 @@
 					    "-t", ctr->title,
 					    "-g", ctr->genre,
 					    "-y", ctr->year,
-					    "-c", ctr->comment,
+					    "-c", ctr->_comment,
 					    "-f", ctr->converted_file,
 					    ctr->orig_file,
 					    NULL);
@@ -2308,7 +2308,7 @@
 				  &ctr->pid,    /* child's PID    */
 				  NULL,         /* child's stdin  */
 				  NULL,         /* child's stdout */
-				  &ctr->stderr, /* child's stderr */
+				  &ctr->std_err,/* child's stderr */
 				  &error);
 
 	    child_pid = ctr->pid;
@@ -2334,7 +2334,7 @@
 		gint status;
 
 		/* set up i/o channel to main thread */
-		ctr->gio_channel = g_io_channel_unix_new (ctr->stderr);
+		ctr->gio_channel = g_io_channel_unix_new (ctr->std_err);
 		g_io_channel_set_flags (ctr->gio_channel,
 					G_IO_FLAG_NONBLOCK, NULL);
 		g_io_channel_set_close_on_unref (ctr->gio_channel, TRUE);
